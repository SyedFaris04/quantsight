# backend/requirements.txt
# ─────────────────────────────────────────────────────
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────

# API server
fastapi
uvicorn[standard]
pydantic

# Data
pandas
numpy
pyarrow

# Machine learning
xgboost
scikit-learn
shap

# Sentiment & Emotion
vaderSentiment
transformers
datasets

# PyTorch — CPU-only build. Plain "torch" on Linux pulls the CUDA build
# (1-2GB+) by default, which is wasted weight with no GPU on Render's free
# tier and can blow past its build time/memory limits. --extra-index-url
# applies file-wide (not just to this line) but is harmless for every other
# package here since PyPI is still checked first/normally for those.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# News fetching
gdeltdoc

# Live prices — Market page Live Mode, Portfolio real-time prices, /live/{ticker}
yfinance

# Utilities
python-multipart
python-dotenv

# AI Assistant — Groq (free tier, Llama 3.3 70B) chat completions
groq
